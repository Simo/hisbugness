<%= form_for(@browser) do |f| %>
  <% if @browser.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@browser.errors.count, "error") %> prohibited this browser from being saved:</h2>

      <ul>
      <% @browser.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :version %><br>
    <%= f.text_field :version %>
  </div>
  <div class="field">
    <%= f.label :issue_id %><br>
    <%= f.text_field :issue_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
